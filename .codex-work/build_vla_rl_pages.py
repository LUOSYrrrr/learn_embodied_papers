#!/usr/bin/env python3
"""Generate the verified VLA+RL paper-note pages from hand-checked PDF evidence."""

from pathlib import Path
from html import escape

ROOT = Path(__file__).resolve().parents[1]

PAPERS = [
dict(slug="rt-2", title="RT-2: Vision-Language-Action Models Transfer Web Knowledge to Robotic Control", short="RT-2", author="Brohan 等", year="2023", source="arXiv:2307.15818", zotero="5PYNLQIY", angle="把机器人动作写成语言 token，让同一个 VLM 同时学习网页视觉语言任务与闭环控制。它不是 RL 论文，却是后续 VLA+RL 必须先站稳的基座。", problem="网页预训练拥有语义知识，但普通机器人策略只会拟合低层动作；RT-2 要把两种能力放进同一输出空间。", steps=["把 7-DoF 连续动作离散化为文本 token", "混合机器人轨迹与 Internet-scale VQA 数据", "共享 VLM 参数做自回归联合训练", "推理时将 token 反解为动作并闭环执行"], evidence="论文进行了约 6,000 次机器人评测，报告了新物体、新指令和基础语义推理上的泛化提升。", limit="语义能力来自网页预训练，但可执行的物理技能仍主要受机器人数据覆盖范围限制。", quote="We express the actions as text tokens.", cite="Abstract, p.1", formula=r"\mathcal{L}_{RT2}=-\sum_t\log p_\theta(y_t\mid y_{<t},I,x)", terms=[("y_t","语言或动作 token"),("I,x","图像与任务指令"),("p_\\theta","共享 VLM 的自回归分布")]),
dict(slug="rl-vla-generalization", title="What Can RL Bring to VLA Generalization? An Empirical Study", short="RL → VLA 泛化", author="Liu 等", year="2025/2026", source="arXiv:2505.19789", zotero="96K6JKE6", angle="这篇论文不再只问 RL 能否提高训练任务成功率，而是把泛化拆成视觉、语义与执行三个维度，系统比较 PPO、DPO、GRPO 和 SFT。", problem="SFT 的开环动作拟合会在闭环分布偏移下累积误差，但此前缺少 VLA 泛化维度上的系统实证。", steps=["建立视觉、语义、执行三类分布外测试", "从同一 VLA 基座出发做 SFT 与多种 RL 微调", "用闭环 rollout 收集任务回报", "比较 PPO、DPO、GRPO 对各维度的真实增益"], evidence="论文报告 PPO 明显改善语义理解与执行鲁棒性，同时视觉鲁棒性与 SFT 大体相当；PPO 比 DPO/GRPO 更有效。", limit="结论来自特定 VLA、任务和奖励设置，不能直接外推为‘任何 RL 都提升任何泛化’。", quote="PPO significantly enhances generalization in semantic understanding and execution robustness.", cite="Abstract, p.1", formula=r"L^{clip}=\mathbb{E}[\min(r_tA_t,\operatorname{clip}(r_t,1-\epsilon,1+\epsilon)A_t)]", terms=[("r_t","新旧策略概率比"),("A_t","闭环回报估计出的优势"),("\\epsilon","限制单次更新幅度")]),
dict(slug="expo-ft", title="EXPO-FT: Sample-Efficient Reinforcement Learning Finetuning for Vision-Language-Action Models", short="EXPO-FT", author="Dong 等", year="2026", source="arXiv:2605.25477", zotero="92GN8NUH", angle="EXPO-FT 把 VLA 推理、真机采样和训练组织成可快速回滚的在线系统，让预训练先验真正转化成分钟级 RL 微调。", problem="从零训练 RL 太慢，而直接全量微调 VLA 又容易不稳定；部署需要在很少真机交互下把成功率补到可用水平。", steps=["从预训练 VLA 与少量任务数据开始", "训练/推理服务器持续发布策略版本", "Q 引导采样与人工编辑提升有效探索", "用在线成功信号迭代动作块"], evidence="在插花、走线、台球等任务上，论文报告平均 19.1 分钟在线数据达到每项 30/30 成功。", limit="样本效率依赖任务复位、成功判定和系统并发设计；不能只看算法损失而忽略整套工程。", quote="perfect task performance across all evaluated tasks within an average of 19.1 minutes", cite="Abstract, p.1", formula=r"J(\theta)=\mathbb{E}_{\pi_\theta}\left[\sum_t\gamma^tr_t\right]", terms=[("r_t","真机任务反馈"),("\\gamma","长程回报折扣"),("\\pi_\\theta","待微调 VLA 策略")]),
dict(slug="dygro-vla", title="DyGRO-VLA: Cross-Task Scaling of Vision-Language-Action Models via Dynamic Grouped Residual Optimization", short="DyGRO-VLA", author="Lin 等", year="2026", source="arXiv:2605.17486", zotero="8P4SRGLT", angle="DyGRO-VLA 把重点从单任务涨点移到多任务不互相破坏：先学共享表征，再用动态分组的 RL residual 决定哪些更新该共享。", problem="普通 RL 微调容易把 generalist 变成只会少数任务的 specialist，根因是跨任务表征被相互干扰。", steps=["离线阶段抽取跨任务共享潜变量", "按信息量冻结或保护共享表征", "为任务组建立多支 RL residual", "动态路由并组合 residual 完成策略更新"], evidence="论文在 LIBERO、RoboTwin2 和真实机器人上报告了多任务训练及分布偏移下的一致提升。", limit="动态路由需要足够任务多样性；若任务定义或分组本身偏差大，residual 仍可能固化错误边界。", quote="strategically mitigating adverse interference on the learned representations", cite="Abstract, p.1", formula=r"\Delta\theta=\sum_k g_k(z,\tau)\,R_k", terms=[("R_k","第 k 组任务的策略 residual"),("g_k","动态路由权重"),("z,\\tau","共享表征与任务上下文")]),
dict(slug="bora", title="BORA: Bridging Offline Reinforcement Learning and Online Residual Adaptation for Real-World Dexterous VLA Models", short="BORA", author="Chen 等", year="2026", source="arXiv:2605.30226", zotero="MX6TU48C", angle="BORA 先用离线数据学动作条件 critic，再冻结 VLA，用人类介入驱动的轻量 residual 修正真机误差，专门面向高自由度灵巧手。", problem="灵巧手动作维度高、遮挡多，直接在线探索既危险又容易让 critic 只记背景。", steps=["一致性策略用少量去噪步生成动作块", "critic 同时读取认知 token 与动作块", "离线 IQL/PPO 提炼可靠意图", "冻结基座，用 HiL residual 做在线小修正"], evidence="五个真实灵巧任务上平均成功率绝对提升 33%，未见物体泛化最高提升 43%。", limit="需要人类介入信号与可靠的离线 critic；安全成本被降低但没有消失。", quote="freezes the VLA base and introduces a lightweight, Human-in-the-Loop residual adaptation", cite="Abstract, p.1", formula=r"A_{final}=A_{base}+\lambda_{res}\,\pi_{res}(s,A_{base},z)", terms=[("A_{base}","冻结 VLA 给出的动作块"),("\\pi_{res}","在线学习的轻量残差策略"),("\\lambda_{res}","限制修正幅度的安全系数")]),
dict(slug="self-improving-vla", title="Self-Improving Vision-Language-Action Models with Data Generation via Residual RL", short="PLD Self-Improving VLA", author="Xiao 等", year="2025", source="arXiv:2511.00091", zotero="IG59EPPZ", angle="PLD 不把 RL 当最终部署策略，而把它当‘失败恢复数据生成器’：Probe 找失败区，Learn 学 residual，Distill 再蒸馏回通用 VLA。", problem="人类演示昂贵，而且常常没有覆盖基座真实部署时会走进的失败状态。", steps=["Probe：冻结 VLA 并定位失败区域", "Learn：训练任务级 residual 专家接管失败状态", "混合 rollout 收集恢复与正常轨迹", "Distill：用 SFT 把数据蒸馏回 generalist"], evidence="论文报告 LIBERO 近 99%、SimplerEnv 超过 50% 增益，并在 Franka/YAM 真机任务达到 100%。", limit="自提升质量取决于 residual 专家能否覆盖关键失败区；蒸馏仍可能遗失专家的少数行为。", quote="RL-generated, policy-aligned data can surpass teleoperation-only demonstrations", cite="Abstract, p.1", formula=r"\theta'\leftarrow\arg\min_\theta\mathbb{E}_{(o,g,a)\sim D_{PLD}}\ell(\pi_\theta(o,g),a)", terms=[("D_{PLD}","由 residual RL 策略对齐生成的数据"),("\\ell","动作头对应的 SFT 损失"),("\\theta'","蒸馏后的通用 VLA")]),
dict(slug="arfm", title="Balancing Signal and Variance: Adaptive Offline RL Post-Training for VLA Flow Models", short="ARFM", author="Zhang 等", year="2025", source="arXiv:2509.04063", zotero="CUR3LF8J", angle="ARFM 的重点不是把 return 硬塞进 flow loss，而是自适应调节 RL 信号强度，在保留优势信息与控制梯度方差之间找平衡。", problem="flow matching 一次建模整段向量场，粗暴回报加权容易让梯度噪声盖过原有生成能力。", steps=["从离线轨迹计算 RL 优势", "估计 RL 信号带来的偏差与方差", "自适应生成能量/权重系数", "用加权 flow matching 更新 VLA 动作头"], evidence="仿真和真机实验显示了泛化、扰动鲁棒、少样本与持续学习方面的提升。", limit="效果依赖离线优势估计质量；critic 偏差仍会通过权重进入 flow 训练。", quote="adaptively balances RL advantage preservation and flow loss gradient variance", cite="Abstract, p.1", formula=r"\mathcal{L}_{ARFM}=w(A,\operatorname{Var}[g])\,\mathcal{L}_{FM}", terms=[("A","离线轨迹的优势信号"),("\\operatorname{Var}[g]","加权后梯度方差"),("w","自适应偏差—方差权衡系数")]),
dict(slug="co-rft", title="CO-RFT: Efficient Fine-Tuning of Vision-Language-Action Models through Chunked Offline Reinforcement Learning", short="CO-RFT", author="Huang 等", year="2025", source="arXiv:2508.02219", zotero="5F97K7YX", angle="CO-RFT 直接把 VLA 的 action chunk 当 RL 决策单元，扩展 TD 学习去处理块内信用分配，用几十条演示完成离线微调。", problem="普通一步 TD 与 VLA 一次输出多步动作的结构不匹配，导致样本效率和训练稳定性差。", steps=["30–60 条演示先做全参 IL 初始化", "critic 同时编码图像、语言、本体状态", "对 action chunk 做块内 TD 传播", "离线策略改进后直接真机评估"], evidence="真实环境报告成功率提升 57%、周期时间下降 22.3%，未见位置成功率达到 44.3%。", limit="极少数据使结果对演示覆盖度与奖励标注敏感；不是所有长 action chunk 都适合相同 bootstrap。", quote="extend temporal difference learning to incorporate action chunking", cite="Abstract, p.1", formula=r"y_{t,i}=r_{t+i}+\gamma V(s_{t,i+1})", terms=[("i","动作块内部位置"),("y_{t,i}","块内第 i 步 TD 目标"),("V","由 critic 估计的后续价值")]),
dict(slug="rankq", title="RankQ: Offline-to-Online Reinforcement Learning via Self-Supervised Action Ranking", short="RankQ", author="Ni 等", year="2026", source="arXiv:2605.11151", zotero="BZ7M7Q8S", angle="RankQ 不把所有数据外动作一律压低，而是学习动作相对排序，让 Q 地形的梯度真正指向更高质量行为。", problem="传统保守离线 RL 像行为克隆锚点：能防 Q 过估计，却会妨碍在线阶段超越次优数据。", steps=["离线 TD 学习建立基础 Q", "从轨迹构造自监督动作偏好对", "多项 ranking loss 塑造局部 Q 地形", "在线交互沿更可靠梯度继续改进"], evidence="VLA 低数据仿真平均领先次优方法 42.7%；真机堆叠成功率由 43.1% 提升到 88.9%。", limit="排序信号决定 Q 地形方向；偏好对若受数据偏差污染，在线探索会被系统性误导。", quote="learning relative action preferences rather than uniformly penalizing unseen actions", cite="Abstract, p.1", formula=r"\mathcal{L}_{rank}=\max(0,m-[Q(s,a^+)-Q(s,a^-)])", terms=[("a^+,a^-","排序中的较优与较差动作"),("m","希望保持的价值间隔"),("Q","指导在线改进的 critic")]),
dict(slug="rise-compositional-world-model", title="RISE: Self-Improving Robot Policy with Compositional World Model", short="RISE", author="Yang 等", year="2026", source="arXiv:2602.11075", zotero="D4HSYHH4", angle="RISE 把昂贵真机 RL 搬进想象空间：动力学模型预测多视角未来，进度价值模型给想象轨迹打分，再用优势条件更新策略。", problem="接触任务需要在线纠错，但真机采样串行、危险且复位昂贵；单一生成模型又很难同时做好画面与价值。", steps=["可控动力学模型预测动作条件未来", "独立 progress value model 评估进展", "在 compositional world model 中批量 rollout", "用想象优势做策略自提升并回真机验证"], evidence="动态积木分拣、背包整理、盒子闭合分别报告超过 +35%、+45%、+35% 的绝对提升。", limit="想象训练只有在动力学可控且价值排序可靠时成立；模型偏差可能被策略主动利用。", quote="continuously generates imaginary rollouts, estimates advantages, and updates the policy", cite="Abstract, p.1", formula=r"A(o_t,a_t,\ell)=\frac1H\sum_{k=1}^H[V(\hat{o}_{t+k},\ell)-V(o_t,\ell)]", terms=[("\\hat{o}_{t+k}","动力学模型想象的未来观测"),("V","任务进度价值模型"),("H","动作块/想象窗口长度")]),
dict(slug="reinflow", title="ReinFlow: Fine-tuning Flow Matching Policy with Online Reinforcement Learning", short="ReinFlow", author="Zhang 等", year="2025", source="NeurIPS 2025 · arXiv:2505.22094", zotero="6LLMWBFR", angle="ReinFlow 给确定性的 flow 路径注入可学习高斯噪声，把它变成离散 Markov 过程，从而得到可计算 likelihood 和可控探索。", problem="flow ODE 的路径本来是确定的，策略梯度既缺探索又难稳定计算动作概率，少步推理时尤其突出。", steps=["从预训练 rectified/shortcut flow 开始", "每个积分步注入可学习噪声", "用高斯转移写出精确 log probability", "调用 PPO 等策略梯度联合更新速度场与噪声网"], evidence="报告 locomotion 回报平均净增 135.36%，相对 DPPO 节省 82.63% wall time；操纵成功率平均净增 40.34%。", limit="可学习噪声要同时服务探索与似然计算；正则强度和去噪步数仍是敏感超参数。", quote="injects learnable noise into a flow policy's deterministic path", cite="Abstract, p.1", formula=r"a_{k+1}=a_k+v_\theta(t_k,a_k,o)\Delta t+\sigma_\phi(t_k,a_k,o)\epsilon", terms=[("v_\\theta","预训练 flow 的速度场"),("\\sigma_\\phi\\epsilon","可学习探索噪声"),("a_k","去噪/积分链中的中间动作")]),
dict(slug="dppo", title="Diffusion Policy Policy Optimization", short="DPPO", author="Ren 等", year="2024", source="arXiv:2409.00588", zotero="UPIQJGUP", angle="DPPO 把完整扩散去噪链视为策略内部的 MDP，再把环境奖励传播到各去噪步骤，用 PPO 微调预训练 diffusion policy。", problem="扩散动作头表达力强，但长去噪链让策略梯度反传昂贵且容易不稳定。", steps=["加载预训练 diffusion policy", "把每个去噪转移视为内部动作", "环境 rollout 计算 advantage", "在去噪链上做 clipped policy-gradient 更新"], evidence="论文报告在多类机器人基准上总体性能和效率领先，并展示仿真训练策略零样本部署到真实长程装配。", limit="反向传播穿过整条去噪链有显存与计算开销；少步 flow 策略出现后，DPPO 不再总是效率最优。", quote="structured and on-manifold exploration, stable training, and strong policy robustness", cite="Abstract, p.1", formula=r"L_{DPPO}=\mathbb{E}\sum_k\min(r_kA,\operatorname{clip}(r_k)A)", terms=[("k","扩散去噪步骤"),("r_k","对应去噪转移的新旧概率比"),("A","最终环境回报产生的优势")]),
dict(slug="iql", title="Offline Reinforcement Learning with Implicit Q-Learning", short="IQL", author="Kostrikov 等", year="2021/2022", source="ICLR 2022 · arXiv:2110.06169", zotero="E37AXX5B", angle="IQL 不查询数据集外动作，而用上 expectile 从数据动作的 Q 分布中隐式逼近最好动作，再用优势加权行为克隆抽取策略。", problem="离线 RL 想超过行为策略，却一评估未见动作就容易因分布外 Q 误差崩溃。", steps=["只对数据集动作拟合 Q", "expectile 回归得到偏向高价值的 V", "用 Bellman backup 更新 Q", "优势加权行为克隆提取策略并可继续在线微调"], evidence="论文在 D4RL 上达到当时强结果，并展示离线初始化后继续在线微调的能力；它也是多篇 VLA 离线 RL 的基础。", limit="IQL 的保守性来自数据支持而非魔法；数据中没有可泛化的好动作时，上 expectile 也无法凭空创造技能。", quote="never needs to evaluate actions outside of the dataset", cite="Abstract, p.1", formula=r"L_V=\mathbb{E}_{(s,a)\sim D}[|\tau-\mathbf{1}_{Q-V<0}|(Q(s,a)-V(s))^2]", terms=[("\\tau","expectile 水平，越大越偏向高价值动作"),("Q-V","数据动作相对状态价值的优势"),("D","固定离线数据集")]),
dict(slug="awac", title="AWAC: Accelerating Online Reinforcement Learning with Offline Datasets", short="AWAC", author="Nair 等", year="2020", source="CoRL 2020 · arXiv:2006.09359", zotero="9YDHBHWS", angle="AWAC 用 off-policy critic 学价值，再按指数优势加权最大似然更新 actor，能先吃历史数据、再平滑转在线。", problem="纯离线方法难继续在线提高，纯在线方法又浪费已有演示并承担高探索成本。", steps=["用离线/在线混合 replay 学 Q", "计算数据动作相对当前策略的优势", "优势越高，行为克隆权重越大", "持续加入新交互并快速在线改进"], evidence="模拟与真实机器人实验显示，历史数据显著缩短了灵巧手、开抽屉和阀门旋转等技能的学习时间。", limit="指数权重会放大 critic 误差，温度与 replay 数据配比需要谨慎控制。", quote="advantage weighted actor critic enables rapid learning of skills", cite="Abstract, p.1", formula=r"\mathcal{L}_{actor}=-\mathbb{E}_{(s,a)\sim D}[e^{A(s,a)/\lambda}\log\pi_\theta(a\mid s)]", terms=[("A(s,a)","critic 估计的动作优势"),("\\lambda","控制权重尖锐度的温度"),("D","历史与在线经验的混合数据")]),
dict(slug="hil-serl", title="Precise and Dexterous Robotic Manipulation via Human-in-the-Loop Reinforcement Learning", short="HIL-SERL", author="Luo 等", year="2024/2025", source="CoRL 2024 · arXiv:2410.21845", zotero="IVIAB7U8", angle="HIL-SERL 把人类纠正当成高价值训练信号，而非失败后的惩罚：演示、纠正、RLPD、视觉预训练和安全控制器一起构成真机 RL 系统。", problem="真实 RL 的稀疏奖励、危险探索和复位成本使复杂双臂/精密操作长期被认为不实用。", steps=["少量演示初始化 replay 与成功分类器", "预训练视觉编码器支持像素策略", "人类在错误苗头出现时给出纠正", "off-policy RLPD 同时利用演示、纠正与自主数据"], evidence="多项精密和动态真机任务在 1–2.5 小时内接近满成功率，平均成功率约为模仿学习的 2 倍，执行快 1.8 倍。", limit="它证明的是精心集成系统的可行性，不是单一算法插件；人类监督和任务工程仍有成本。", quote="integrates demonstrations and human corrections, efficient RL algorithms", cite="Abstract, p.1", formula=r"J(\pi)=\mathbb{E}_\pi\left[\sum_{t=0}^{H}\gamma^tr(s_t,a_t)\right]", terms=[("r","成功分类器与任务奖励"),("\\pi","接收图像和本体状态的策略"),("H","真实任务时域")]),
]

POINTS = {
"arfm":[(50,12),(50,36),(50,58),(50,78)], "awac":[(18,32),(43,32),(68,32),(84,32)],
"bora":[(27,45),(48,42),(70,42),(55,70)], "co-rft":[(13,38),(40,38),(67,38),(87,38)],
"dppo":[(14,32),(37,32),(61,32),(84,32)], "dygro-vla":[(18,38),(41,38),(66,38),(86,38)],
"expo-ft":[(15,45),(40,45),(66,45),(86,45)], "hil-serl":[(15,40),(40,40),(66,40),(86,40)],
"iql":[(18,42),(45,42),(76,42),(53,70)], "rankq":[(18,38),(48,38),(78,38),(50,72)],
"reinflow":[(18,27),(50,27),(82,27),(50,64)], "rise-compositional-world-model":[(17,35),(47,35),(78,35),(50,72)],
"rl-vla-generalization":[(15,25),(48,25),(80,25),(50,68)], "rt-2":[(14,40),(38,40),(64,40),(87,40)],
"self-improving-vla":[(15,35),(40,35),(67,35),(87,35)],
}

DEEP = {
"rt-2": dict(unit="输入是当前相机观测与语言指令，输出是离散化后的平移、旋转、夹爪与终止 token；动作和普通文本共享自回归词表。", feedback="RT-2 没有 critic 或环境奖励。监督来自机器人轨迹中的动作 token，以及 VQA/语言任务中的文本 token。", update="PaLI-X 或 PaLM-E 视觉语言骨干与动作输出完全共享参数，通过 co-fine-tuning 保住网页知识并学习控制。", setup="模型最高扩展到 55B 参数；机器人数据沿用 RT-1 系列，评测约 6,000 次真机 rollout，分别考察熟悉任务、未见物体、未见语义和组合推理。", compare="相对只在机器人数据上训练的策略，RT-2 的主要优势是把网页语义迁移到动作选择；它并没有突破机器人数据没有覆盖的运动技能。", reproduce="先确认动作离散化边界与 token 顺序；混合数据比例必须避免语言任务淹没机器人动作；部署时要严格反量化并保持闭环频率。", takeaway="RT-2 给后续 VLA+RL 留下的核心问题是：语义先验已经很好，如何用闭环反馈修正执行，而不破坏这些先验。"),
"rl-vla-generalization": dict(unit="研究以 warmed-up OpenVLA 为共同初始化，把泛化拆为 Vision、Semantics 与 Execution，而不是只汇报一个平均成功率。", feedback="SFT 只看专家动作；PPO 使用环境 rollout 的 advantage。DPO/GRPO 被作为从 LLM 后训练迁来的替代反馈形式。", update="实验采用参数高效微调，并让 actor 与 critic 共享视觉语言表征；关键工程是稳定 PPO 的价值预测与 rollout 数据覆盖。", setup="在仿真和真实环境中构造颜色/背景等视觉变化、未见物体与语言组合、位置与扰动等执行变化；LoRA rank 32 用于保证微调容量。", compare="PPO 在语义和执行维度优于 SFT，视觉维度大体持平；DPO/GRPO 并未自然继承 LLM 场景中的优势。", reproduce="IND 与 OOD 必须只改变一个因素；SFT/RL 要共享初始化、数据预算与评测次数；同时记录轨迹覆盖范围而不只记录终局成功。", takeaway="RL 的价值更像‘扩大可恢复状态分布’，不是让视觉编码器自动获得任意域泛化。"),
"expo-ft": dict(unit="策略是预训练 VLA，训练系统把动作 chunk、环境状态、成功信号与人类 correction 写入 replay buffer。", feedback="稀疏任务成功提供主奖励，Q 函数帮助筛选高价值动作；需要时由人工编辑动作，减少真机无效探索。", update="训练服务器与推理服务器解耦，异步更新动作模块并持续发布策略；高 update-to-data ratio 充分利用每分钟真机数据。", setup="包含 8 个高精度或动态真机任务，如插花、插头通电、台球入袋与灯串布线；每个任务以 30 次独立评测检查可靠性。", compare="区别于 RL-from-scratch，它把 VLA 先验视为强行为分布；区别于全参 VLA RL，它把稳定系统和样本复用放在首位。", reproduce="必须记录真实机器人分钟数而非仅梯度步数；成功检测、复位和人工 correction 协议要公开；策略发布应支持回滚。", takeaway="EXPO-FT 的贡献一半是算法，一半是数据面与训练面解耦的系统设计。"),
"dygro-vla": dict(unit="共享 VLA 表征先通过 information bottleneck 学到跨任务因子；在线阶段用多组 residual 表达不同任务的更新方向。", feedback="各任务的环境回报产生 RL residual，router 根据任务和共享潜变量选择、混合这些 residual。", update="两阶段训练避免从一开始就让任务梯度互相拉扯；基座和共享表征受到保护，动态 residual 负责局部适配。", setup="在 LIBERO-Spatial/Object/Goal/Long、RoboTwin2 代表任务和真实机器人上评估，并专门检查多任务数增加后的遗忘与分布偏移。", compare="普通单 residual 或每任务独立微调要么相互干扰、要么无法共享；DyGRO 用动态分组在共享和隔离之间找中间点。", reproduce="任务 ID、router 输入和分组数必须做消融；需要同时报告旧任务保持率与新任务提升；t-SNE 只能作辅助证据。", takeaway="多任务 VLA+RL 的主矛盾不是单任务优化强度，而是哪些梯度应该共享。"),
"bora": dict(unit="离线 actor 采用少步一致性动作头；critic 同时读取 VLM cognition tokens、动作 chunk 与块内位置编码。", feedback="离线阶段用 IQL 式价值与 clipped policy improvement；在线阶段使用稀疏成功奖励和人类 intervention 信号。", update="先联合训练离线基座和 action-conditioned critic，再冻结 VLA，仅更新轻量 chunk residual，并继承离线 critic。", setup="五个真实灵巧手任务覆盖高自由度、遮挡与接触变化；同时测试标准设置与未见物体泛化。", compare="传统 decoupled critic 容易抓背景捷径；直接全参在线微调会产生特征漂移。BORA 把语义 token、动作后果和 residual 安全边界绑定起来。", reproduce="动作块内 Q/V 的时间对齐不能错一位；residual 系数要限制修正幅度；必须报告人工介入次数与硬件失败。", takeaway="对高自由度真机，‘冻结强基座 + 继承 critic + 小 residual’比全参 RL 更像可部署方案。"),
"self-improving-vla": dict(unit="基座 VLA 不直接承担探索；每个任务训练轻量 residual specialist，专门接管基座会失败的状态。", feedback="specialist 用稀疏成功奖励学习；hybrid rollout 控制何时由基座或 residual 执行，从而采到恢复动作且贴近部署分布。", update="Stage 1 只学 residual，Stage 2 自动收集 PLD 数据，Stage 3 用普通 SFT 把多任务恢复轨迹蒸馏回通用 VLA。", setup="评测覆盖 LIBERO-90 不同任务覆盖率、SimplerEnv，以及 Franka 与 YAM 双臂真机；还展示 GPU 插拔一小时连续运行。", compare="直接拿基座自己 rollout 的 0/1 REINFORCE 数据质量低；纯人类数据质量高但未必覆盖基座真实失败分布。PLD 试图同时取得二者优点。", reproduce="hybrid takeover 概率和状态覆盖要可视化；蒸馏前要区分成功恢复与偶然成功；对 seen/unseen task 分别汇报。", takeaway="RL 可以是数据引擎，而不是最终部署策略。这条路线往往比长期保留多个 specialist 更易扩展。"),
"arfm": dict(unit="动作头是条件 flow matching 模型，输入语言、图像、本体状态与噪声时间，输出整个动作 chunk 的速度场。", feedback="离线 critic/return 给样本优势，但优势不直接替代 flow 监督，而是经过自适应能量权重调节。", update="方法分析权重带来的 advantage bias 和梯度 variance，再动态限制 RL 信号强度，保留原 flow 向量场的稳定训练。", setup="实验覆盖通用、少样本、扰动鲁棒和持续学习类别，并包含仿真与真实任务；重点观察不同权重机制的稳定性。", compare="固定 return-to-go 加权可能把高回报当成粗粒度条件；ARFM 针对 flow 的整体轨迹建模，显式处理加权梯度方差。", reproduce="先复现不加 RL 的 flow loss；监控有效样本权重、梯度范数与 advantage 分布；不能只调最终成功率。", takeaway="离线 RL 接入 flow VLA 的关键不是奖励越强越好，而是奖励信号的统计质量。"),
"co-rft": dict(unit="VLA 一次预测长度 k 的 action chunk；critic 为每个块位置生成 Q/V，并用位置编码区分块内时间。", feedback="离线数据中的奖励和终止信号形成 shifted bootstrap，让价值从下一个 chunk 倒传到当前 chunk 内各原子动作。", update="先全参 imitation learning 初始化视觉语言骨干与动作头，再做 chunked offline RL；actor 仍受行为克隆项约束。", setup="仅使用约 30–60 条专家演示，在多项真实机器人任务上报告成功率、周期时间和未见位置泛化。", compare="普通 TD3/IQL 把 chunk 当单动作会丢失块内信用；逐帧 RL 又不符合 VLA 的部署接口。CO-RFT 在两者之间做块内 TD。", reproduce="检查 chunk 长度、padding mask 与终止位置；Q/V target 必须处理最后一格跨 chunk bootstrap；小数据结果需要多随机种子。", takeaway="action chunk 不只是工程加速技巧，它改变了 RL 的时间尺度与信用分配。"),
"rankq": dict(unit="critic 学习连续动作空间的 Q landscape；成功和失败轨迹被拆成集合，再构造同状态附近的相对动作顺序。", feedback="TD loss提供绝对回报锚点，多项 ranking loss 提供局部方向，避免简单把所有 OOD 动作压成低值。", update="先离线塑形 Q 地形，再进入在线采样；actor 沿 critic 梯度改进并不断把新数据加入 replay。", setup="先在 D4RL AntMaze/Adroit 验证，再在低数据与高数据 VLA 场景测试，包含 cube stacking 的 sim-to-real。", compare="CQL/Cal-QL 的悲观 Q 面可能在数据边界形成陡墙；RankQ 更关心动作之间的相对顺序，让在线阶段仍有上升方向。", reproduce="成功/失败划分和正负对采样规则必须固定；可视化 Q landscape 只用于诊断，还要验证真实策略梯度与成功率一致。", takeaway="critic 不只是数值预测器，它的局部几何直接决定在线探索会往哪里走。"),
"rise-compositional-world-model": dict(unit="dynamics model 接收历史多视角观测与动作 chunk，预测多步未来；独立 progress value model 给每个想象状态打任务进度分。", feedback="未来进度相对当前状态的增量被平均成 action-chunk advantage，随后作为条件信号更新 VLA。", update="先用真实数据预训练可控 dynamics/value，再 warm-up 策略的 advantage conditioning，最后在想象空间循环采样和更新。", setup="真机任务是动态传送带积木分拣、背包整理和盒子闭合，分别强调动态跟踪、双臂协作与精细接触。", compare="纯视频 world model 只回答‘像不像’，RISE 还需要 action adherence 与 progress ranking；纯真机 RL 则受串行采样和复位限制。", reproduce="必须测 action controllability 而不只测视频质量；value model 要做进度校准；策略是否利用模型漏洞需回真机闭环验证。", takeaway="世界模型用于 RL 时，价值可信度往往比像素逼真度更重要。"),
"reinflow": dict(unit="预训练 rectified/shortcut flow 在 K 个积分步内从高斯噪声生成连续动作，每一步额外加入状态相关高斯噪声。", feedback="环境 advantage 乘以每个 Markov 转移的 log probability；噪声网络既定义可计算 likelihood，也控制探索尺度。", update="速度场与噪声网络可联合更新，外接 PPO 等策略梯度子程序，并用正则限制策略偏离。", setup="覆盖腿式 locomotion、Franka Kitchen 状态任务、RoboMimic 视觉操纵和稀疏长程任务，比较不同去噪步数与 wall time。", compare="DPPO 对扩散去噪链反传；ReinFlow 针对少步 flow，用显式高斯 Markov 转移获得更便宜的 likelihood。", reproduce="逐步核对高斯均值、方差和 log-prob；分别报告 rollout 时间与优化时间；噪声尺度、K 和正则强度需要联合扫描。", takeaway="flow 策略要做在线 RL，首先要回答‘探索随机性从哪里来、概率怎么计算’。"),
"dppo": dict(unit="环境每一步的动作由 K 步反向扩散产生；DPPO 把中间噪声状态视作扩展 MDP 的状态，把去噪转移视作内部策略动作。", feedback="真实环境只在最终动作后给奖励，advantage 被广播到对应的去噪转移，并由各步概率比参与 PPO。", update="从 behavior cloning 的 Diffusion Policy 开始，使用 clipped surrogate、价值学习和正则做 on-policy fine-tuning。", setup="覆盖常见连续控制、像素机器人任务、FurnitureBench 长程装配，并将仿真训练策略零样本部署到硬件。", compare="相对 Q-guided diffusion，DPPO 直接优化环境回报且探索保持在扩散数据流形附近；代价是整条链的反传。", reproduce="扩展 MDP 的 log-prob 与时间索引要正确；区分环境 horizon 和 denoising horizon；记录显存、K 和 rollout 吞吐。", takeaway="生成模型内部采样过程可以被重新解释成决策过程，但这种解释会把计算图长度带进 RL 成本。"),
"iql": dict(unit="训练数据固定为 (s,a,r,s')。Q 只在数据动作上回归，V 用非对称 expectile 从 Q 分布中选择偏高但仍受数据支持的值。", feedback="Bellman target 更新 Q；A=Q−V 只用于给行为克隆加权，不需要从当前策略采样数据外动作。", update="交替优化 V、Q 与 advantage-weighted actor；离线完成后可把同一结构继续用于在线交互。", setup="D4RL 的 MuJoCo locomotion、AntMaze、Franka Kitchen 与 Adroit 是主要基准；论文还测试离线初始化后 1M 在线步。", compare="CQL 显式压低 OOD Q，IQL 选择不查询 OOD；AWAC 同样优势加权，但 IQL 的 expectile V 是其离线稳定性的关键。", reproduce="expectile τ 与 actor 温度 β 要分开调；确认 Q target 不含当前策略采样动作；不同数据质量应分别报告。", takeaway="IQL 的价值在于把‘策略改进’藏进值函数回归，适合成为 VLA 离线 RL 的稳健起点。"),
"awac": dict(unit="off-policy critic 从历史演示与新在线数据共同学习；actor 仍在数据动作上做最大似然，但高优势动作获得指数更大权重。", feedback="TD bootstrap 提供 Q，当前策略下的基线形成 advantage；不需要显式拟合 behavior policy 的密度。", update="离线 replay 先启动，再无缝加入在线数据持续更新 critic 与 actor，因此避免从零探索。", setup="论文用 HalfCheetah 分析离线到在线难点，并在仿真和真实机器人上测试灵巧手、开抽屉、旋转阀门等任务。", compare="纯 BC 不会超越数据，纯离线保守方法在线改进慢；AWAC 用隐式 KL 约束保持数据支持，同时允许价值驱动的在线提升。", reproduce="指数权重需截断或数值稳定化；混合 replay 的离线/在线比例要记录；critic 更新频率会直接改变 actor 权重。", takeaway="AWAC 是理解现代 residual/离线到在线 VLA 方法的最小原型：critic 选数据，actor 仍像加权 SFT。"),
"hil-serl": dict(unit="观测由多视角 RGB 与本体状态组成，策略基于预训练视觉骨干；低层控制器负责把策略输出安全地送到机器人。", feedback="成功分类器产生稠密可学习奖励，人类纠正作为高质量 action 进入 replay；RLPD 同时采样演示和在线数据。", update="actor、critic、环境采样和 replay 分布式运行；人类可以在一条轨迹中多次接管，策略随后离线吸收纠正。", setup="任务包括 Jenga 抽击、锅中翻物、主板/仪表盘/正时皮带装配、IKEA 搭建和双臂交接；常用约 20–30 条演示启动。", compare="HG-DAgger 主要模仿人类纠正，HIL-SERL 还利用失败后的奖励与自主尝试；SERL 没有人类在线纠正，复杂任务更难启动。", reproduce="成功分类器误判会污染所有回报；需统计 intervention rate、训练小时和 cycle time；安全控制与复位流程必须纳入系统。", takeaway="真机 RL 的性能往往来自算法、数据管线、安全控制和人类反馈四者共同闭环。"),
}

def page(p):
    hs=[]; sw=[]
    for i,(step,(x,y)) in enumerate(zip(p["steps"],POINTS[p["slug"]]),1):
        hs.append(f'<div class="hotspot" data-id="s{i}" style="left:{x}%;top:{y}%" data-title="步骤 {i}" data-body="{escape(step)}"></div>')
        sw.append(f'<div class="sw-step" data-hotspot="s{i}"><h4>{i}. {escape(step)}</h4><p>点击图中对应热点，观察它与前后步骤如何连接。</p></div>')
    terms=''.join(f'<dt>${k}$</dt><dd>{escape(v)}</dd>' for k,v in p['terms'])
    dots='<span class="sw-dot"></span>'*4
    return f'''<!DOCTYPE html><html lang="zh-CN"><head><meta charset="UTF-8"><meta name="viewport" content="width=device-width,initial-scale=1"><title>{escape(p['short'])} · VLA+RL 精读</title><link rel="stylesheet" href="../assets/css/style.css"><link rel="stylesheet" href="https://cdn.jsdelivr.net/npm/katex@0.16/dist/katex.min.css"><script defer src="https://cdn.jsdelivr.net/npm/katex@0.16/dist/katex.min.js"></script><script defer src="https://cdn.jsdelivr.net/npm/katex@0.16/dist/contrib/auto-render.min.js" onload="renderMathInElement(document.body,{{delimiters:[{{left:'$$',right:'$$',display:true}},{{left:'$',right:'$',display:false}}]}})"></script><script src="../assets/js/theme.js"></script><script src="../assets/js/interactive.js" defer></script><style>@media(max-width:900px){{.docs-sidebar-left{{display:none!important}}}}</style></head><body><nav class="nav"><a class="nav-logo" href="../index.html">Paper Notes</a><div class="nav-links"><a href="vla-rl-roadmap.html">← VLA+RL 路线</a><a href="https://arxiv.org/abs/{p['source'].split('arXiv:')[-1].split()[0] if 'arXiv:' in p['source'] else ''}">arXiv</a><a href="../pdfs/vla-rl/{p['slug']}.pdf">📄 PDF</a></div></nav><div class="page"><div class="docs-layout"><aside class="docs-sidebar-left"><div class="ds-title">{escape(p['short'])} · {p['year']}</div><a class="ds-leaf-top" href="#question">§1 问题</a><a class="ds-leaf-top" href="#method">§2 方法</a><a class="ds-leaf-top" href="#objective">§3 目标</a><a class="ds-leaf-top" href="#evidence">§4 证据</a><a class="ds-leaf-top" href="#limits">§5 边界</a></aside><main class="docs-content"><div class="docs-breadcrumb"><a href="../index.html">paper-notes</a><span class="sep">/</span><a href="vla-rl-roadmap.html">VLA+RL</a><span class="sep">/</span>{escape(p['short'])}</div><h1>{escape(p['title'])}</h1><p style="color:var(--text3);font-family:var(--font-mono);font-size:12px;margin-top:-12px">{p['author']} · {p['year']} · {p['source']} · Zotero {p['zotero']}</p><div class="cn-read"><div class="cn-label">一句话抓住论文</div><p>{p['angle']}</p></div><h2 id="question">§1 为什么需要它</h2><div class="cn-read"><div class="cn-label">承上启下</div><p>VLA 基座解决“能不能动作”，这一节继续追问“如何利用回报让它在闭环里更可靠”。{p['problem']}</p></div><details class="en-quote"><summary>展开原文 · 核心主张</summary><div class="quote-body"><p>“{escape(p['quote'])}”</p><div class="quote-cite">— {p['cite']}</div></div></details><h2 id="method">§2 方法信号怎么走</h2><div class="cn-read"><div class="cn-label">从问题到结构</div><p>上一节定位了失败来源；下面沿论文原图把训练信号拆成四步，重点看 RL 改了哪个模块、哪些先验被保留。</p></div><div class="fig-interactive panel-below"><div class="fig-block"><div class="hotspot-wrap" data-hotspot-group="method-fig"><img src="../assets/figures/{p['slug']}/fig-method.png" alt="{escape(p['short'])} paper figure">{''.join(hs)}</div><div class="fig-cap"><span class="fig-num">论文原图 · 450 DPI 裁剪</span>{escape(p['short'])} 的方法或关键分析图。<span class="fig-cn">点击热点按训练顺序阅读。</span></div></div><div class="hotspot-panel"></div></div><div class="step-walker" data-hotspot-group="method-fig"><div class="sw-header"><div class="sw-title">跟着训练信号走一遍</div><div class="sw-controls"><button class="sw-btn prev">←</button><span class="sw-counter"></span><button class="sw-btn next">→</button></div></div><div class="sw-progress">{dots}</div><div class="sw-body">{''.join(sw)}</div></div><h2 id="objective">§3 把直觉落到目标函数</h2><div class="cn-read"><div class="cn-label">公式先讲人话</div><p>上图说明模块如何连接，这个公式则压缩了论文真正优化的量。先认清每个符号，再判断奖励、数据支持与预训练先验之间的权衡。</p></div><div class="math-block">$${p['formula']}$$<span class="math-label">论文核心目标的教学化表达</span></div><div class="math-explain"><div class="me-title">逐项拆解</div><dl>{terms}</dl></div><h2 id="evidence">§4 论文拿出了什么证据</h2><div class="cn-read"><div class="cn-label">不要只看涨点</div><p>{p['evidence']}</p></div><div class="callout blue"><div class="callout-label">读数方式</div><p>优先检查训练数据量、是否真机、是否闭环、是否和同一预训练基座比较；不同设置的成功率不能直接横比。</p></div><h2 id="limits">§5 适用边界</h2><div class="cn-read"><div class="cn-label">我的边界判断</div><p>{p['limit']}</p></div><div class="quiz" data-correct="b" data-explain="这篇工作的关键不是泛泛地‘用了 RL’，而是让回报以与 VLA 动作表示相匹配的方式进入训练。" data-explain-wrong="回到方法图，找出被 RL 直接改变的模块。"><div class="q-label">理解检查</div><div class="q-text">阅读这篇论文时最该追踪哪条因果链？</div><div class="q-options"><button class="q-opt" data-k="a">模型名字 → 参数量 → 海报颜色</button><button class="q-opt" data-k="b">反馈信号 → 策略更新 → 闭环行为 → 泛化证据</button><button class="q-opt" data-k="c">作者单位 → 引用数 → 下载量</button><button class="q-opt" data-k="d">只看最终成功率</button></div><div class="q-feedback"></div></div></main></div></div></body></html>'''

def page(p):
    d = DEEP[p["slug"]]
    hotspots, steps = [], []
    for i, (step, (x, y)) in enumerate(zip(p["steps"], POINTS[p["slug"]]), 1):
        hotspots.append(f'<div class="hotspot" data-id="s{i}" style="left:{x}%;top:{y}%" data-title="步骤 {i}" data-body="{escape(step)}"></div>')
        steps.append(f'<div class="sw-step" data-hotspot="s{i}"><h4>{i}. {escape(step)}</h4><p>看图定位这一步的输入、输出，以及它怎样把反馈传到下一阶段。</p></div>')
    terms = ''.join(f'<dt>${k}$</dt><dd>{escape(v)}</dd>' for k, v in p['terms'])
    arxiv = p['source'].split('arXiv:')[-1].split()[0] if 'arXiv:' in p['source'] else ''
    return f'''<!DOCTYPE html>
<html lang="zh-CN"><head><meta charset="UTF-8"><meta name="viewport" content="width=device-width,initial-scale=1">
<title>{escape(p['short'])} · VLA+RL 详细精读</title><link rel="stylesheet" href="../assets/css/style.css">
<link rel="stylesheet" href="https://cdn.jsdelivr.net/npm/katex@0.16/dist/katex.min.css"><script defer src="https://cdn.jsdelivr.net/npm/katex@0.16/dist/katex.min.js"></script><script defer src="https://cdn.jsdelivr.net/npm/katex@0.16/dist/contrib/auto-render.min.js" onload="renderMathInElement(document.body,{{delimiters:[{{left:'$$',right:'$$',display:true}},{{left:'$',right:'$',display:false}}]}})"></script>
<script src="../assets/js/theme.js"></script><script src="../assets/js/interactive.js" defer></script><style>.detail-list{{color:var(--text2);line-height:1.8}}.detail-list li{{margin:.35rem 0}}.evidence-table{{width:100%;border-collapse:collapse;margin:14px 0 22px;font-size:13px}}.evidence-table th,.evidence-table td{{padding:10px 12px;border:1px solid var(--border);text-align:left;vertical-align:top}}.evidence-table th{{width:145px;color:var(--accent);font-family:var(--font-mono);background:var(--bg3)}}@media(max-width:900px){{.docs-sidebar-left{{display:none!important}}.evidence-table th{{width:auto}}}}</style></head>
<body><nav class="nav"><a class="nav-logo" href="../index.html">Paper Notes</a><div class="nav-links"><a href="vla-rl-roadmap.html">← VLA+RL 路线</a><a href="https://arxiv.org/abs/{arxiv}">arXiv</a><a href="../pdfs/vla-rl/{p['slug']}.pdf">📄 PDF</a></div></nav>
<div class="page"><div class="docs-layout"><aside class="docs-sidebar-left"><div class="ds-title">{escape(p['short'])} · {p['year']}</div><a class="ds-leaf-top" href="#question">§1 问题设定</a><a class="ds-leaf-top" href="#method">§2 方法总览</a><a class="ds-leaf-top" href="#mechanism">§3 机制细拆</a><a class="ds-leaf-top" href="#objective">§4 目标函数</a><a class="ds-leaf-top" href="#setup">§5 训练与评测</a><a class="ds-leaf-top" href="#evidence">§6 结果解读</a><a class="ds-leaf-top" href="#reproduce">§7 复现检查</a><a class="ds-leaf-top" href="#limits">§8 边界与启发</a></aside>
<main class="docs-content"><div class="docs-breadcrumb"><a href="../index.html">paper-notes</a><span class="sep">/</span><a href="vla-rl-roadmap.html">VLA+RL</a><span class="sep">/</span>{escape(p['short'])}</div>
<h1>{escape(p['title'])}</h1><p style="color:var(--text3);font-family:var(--font-mono);font-size:12px;margin-top:-12px">{p['author']} · {p['year']} · {p['source']} · Zotero {p['zotero']}</p>
<div class="cn-read"><div class="cn-label">先给结论</div><p>{p['angle']}</p></div>

<h2 id="question">§1 问题设定：SFT 到底漏掉了什么</h2><div class="cn-read"><div class="cn-label">这一节解决什么</div><p>VLA 预训练与 SFT 解决“从观测到动作的模仿”，但部署是闭环过程：动作会改变下一帧，错误会把策略带到演示没有覆盖的状态。{p['problem']}</p></div>
<p>{escape(d['unit'])}</p><details class="en-quote"><summary>展开原文 · 核心主张</summary><div class="quote-body"><p>“{escape(p['quote'])}”</p><div class="quote-cite">— {p['cite']}</div></div></details>
<div class="module-grid"><div class="module-card"><div class="module-card-name">STATE / INPUT</div><div class="module-card-title">策略看见什么</div><div class="module-card-desc">{escape(d['unit'])}</div></div><div class="module-card"><div class="module-card-name">FEEDBACK</div><div class="module-card-title">监督从哪里来</div><div class="module-card-desc">{escape(d['feedback'])}</div></div></div>

<h2 id="method">§2 方法总览：反馈信号怎么走</h2><div class="cn-read"><div class="cn-label">承上启下</div><p>上一节明确了状态、动作与反馈，现在沿论文原图追踪训练信号。重点不是背模块名，而是判断：哪部分保持预训练先验，哪部分真的被 RL 改写。</p></div>
<div class="fig-interactive panel-below"><div class="fig-block"><div class="hotspot-wrap" data-hotspot-group="method-fig"><img src="../assets/figures/{p['slug']}/fig-method.png" alt="{escape(p['short'])} paper method figure">{''.join(hotspots)}</div><div class="fig-cap"><span class="fig-num">论文原图 · 450 DPI 裁剪</span>{escape(p['short'])} 的方法或关键分析图。<span class="fig-cn">点击热点，按训练信号顺序阅读。</span></div></div><div class="hotspot-panel"></div></div>
<div class="step-walker" data-hotspot-group="method-fig"><div class="sw-header"><div class="sw-title">跟着训练信号走一遍</div><div class="sw-controls"><button class="sw-btn prev">←</button><span class="sw-counter"></span><button class="sw-btn next">→</button></div></div><div class="sw-progress">{'<span class="sw-dot"></span>'*4}</div><div class="sw-body">{''.join(steps)}</div></div>

<h2 id="mechanism">§3 机制细拆：动作、价值与更新边界</h2><div class="cn-read"><div class="cn-label">从架构图到实现</div><p>上图给出了宏观流程，但真正决定训练是否稳定的是三个接口：动作以什么粒度出现、反馈怎样变成可学习信号、哪些参数允许被更新。</p></div>
<div class="module-grid"><div class="module-card"><div class="module-card-name">ACTION UNIT</div><div class="module-card-title">动作表示</div><div class="module-card-desc">{escape(d['unit'])}</div></div><div class="module-card"><div class="module-card-name">CREDIT</div><div class="module-card-title">信用分配</div><div class="module-card-desc">{escape(d['feedback'])}</div></div><div class="module-card"><div class="module-card-name">UPDATE</div><div class="module-card-title">更新范围</div><div class="module-card-desc">{escape(d['update'])}</div></div><div class="module-card"><div class="module-card-name">WHY IT MATTERS</div><div class="module-card-title">与相邻方法的差别</div><div class="module-card-desc">{escape(d['compare'])}</div></div></div>

<h2 id="objective">§4 目标函数：把直觉压缩成可检查的量</h2><div class="cn-read"><div class="cn-label">公式先讲人话</div><p>前一节说清了信号路径，这里把核心优化目标写成教学化公式。读公式时依次检查：回报影响谁、数据支持在哪里、预训练策略受到什么约束。</p></div>
<div class="math-block">$${p['formula']}$$<span class="math-label">论文核心目标的教学化表达；精确实现以 PDF 对应章节为准</span></div><div class="math-explain"><div class="me-title">逐项拆解</div><dl>{terms}</dl></div>
<div class="callout blue"><div class="callout-label">实现桥梁</div><p>{escape(d['update'])}</p></div>

<h2 id="setup">§5 训练与评测：数字是在什么条件下得到的</h2><div class="cn-read"><div class="cn-label">先固定比较口径</div><p>只看最终成功率会隐藏数据预算、仿真/真机差异和基座强弱。本节把论文的实验对象与比较关系放回同一张表。</p></div>
<table class="evidence-table"><tr><th>策略与动作</th><td>{escape(d['unit'])}</td></tr><tr><th>训练反馈</th><td>{escape(d['feedback'])}</td></tr><tr><th>更新方式</th><td>{escape(d['update'])}</td></tr><tr><th>实验覆盖</th><td>{escape(d['setup'])}</td></tr><tr><th>关键对照</th><td>{escape(d['compare'])}</td></tr></table>

<h2 id="evidence">§6 结果怎么读：证据、因果与可迁移结论</h2><div class="cn-read"><div class="cn-label">论文实际支持什么</div><p>{p['evidence']}</p></div>
<p>{escape(d['compare'])}</p><div class="callout blue"><div class="callout-label">路线定位</div><p>{escape(d['takeaway'])}</p></div>
<div class="quiz" data-correct="c" data-explain="只有控制初始化、数据预算与闭环评测口径，才能把性能差异较可靠地归因于 RL 机制。" data-explain-wrong="单看最终成功率不能排除基座、数据量和评测条件差异。"><div class="q-label">理解检查 · 实验</div><div class="q-text">比较这篇方法与另一篇 VLA+RL 工作时，最先应该对齐什么？</div><div class="q-options"><button class="q-opt" data-k="a">论文发布日期</button><button class="q-opt" data-k="b">模型名称长度</button><button class="q-opt" data-k="c">基座、交互预算与闭环评测设置</button><button class="q-opt" data-k="d">作者单位数量</button></div><div class="q-feedback"></div></div>

<h2 id="reproduce">§7 复现与工程检查</h2><div class="cn-read"><div class="cn-label">从论文结论回到代码</div><p>前一节判断了证据强度，复现时还要把训练信号对齐、时间尺度和数据统计逐项落地，否则“同名算法”也可能得到完全不同的结果。</p></div>
<ul class="detail-list"><li>{escape(d['reproduce'])}</li><li>先跑通不加 RL 的预训练/SFT 基线，并保存逐任务成功率与失败视频。</li><li>同时记录环境步数、真实墙钟时间、人工介入次数、更新次数和推理延迟。</li><li>把奖励、终止、action chunk mask 与归一化统计做成可视化单元测试。</li></ul>

<h2 id="limits">§8 适用边界与研究启发</h2><div class="cn-read"><div class="cn-label">我的边界判断</div><p>{p['limit']}</p></div><p>{escape(d['takeaway'])}</p>
<div class="quiz" data-correct="b" data-explain="核心因果链是反馈信号如何改变策略，再如何改善闭环行为；‘使用 RL’本身不是解释。" data-explain-wrong="回到 §2–§4，找出被更新的模块和对应目标。"><div class="q-label">理解检查 · 方法</div><div class="q-text">读完这篇论文后，最应该能复述哪条链？</div><div class="q-options"><button class="q-opt" data-k="a">名字 → 参数量 → 发表年份</button><button class="q-opt" data-k="b">反馈 → 信用分配 → 参数更新 → 闭环证据</button><button class="q-opt" data-k="c">摘要 → 海报 → 项目视频</button><button class="q-opt" data-k="d">只背最终成功率</button></div><div class="q-feedback"></div></div>
</main></div></div></body></html>'''

for paper in PAPERS:
    out=ROOT/'papers'/f"{paper['slug']}.html"
    out.write_text(page(paper))
    print(out)
